import React from 'react';

const FractalSplatTest: React.FC = () => (
  <div style={{ padding: 24, color: '#bda4ff' }}>
    FractalSplatTest — placeholder. Original page deleted; restore from git or rebuild.
  </div>
);

export default FractalSplatTest;
