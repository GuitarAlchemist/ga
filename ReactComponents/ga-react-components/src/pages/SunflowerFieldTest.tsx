import React from 'react';

const SunflowerFieldTest: React.FC = () => (
  <div style={{ padding: 24, color: '#bda4ff' }}>
    SunflowerFieldTest — placeholder. Original page deleted; restore from git or rebuild.
  </div>
);

export default SunflowerFieldTest;
