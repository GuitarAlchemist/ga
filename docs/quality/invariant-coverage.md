# Invariant coverage report

Matrix: **35 invariants × 37 exemplars** (rank = **3**).

✘ **Suboptimal** — see details below.

## Rank deficiency: 32

32 invariant(s) are linearly dependent on the others over GF(2). At least one can be removed (or derived) without losing discriminating power.

## Strict duplicates

Invariant pairs with identical firing signatures — first candidates for removal.

| Keep | Remove / redefine |
|---|---|
| #16 | #17 |
| #16 | #20 |
| #16 | #24 |
| #17 | #20 |
| #17 | #24 |
| #20 | #24 |

## Orphan invariants (no firings observed)

| # | Domain | Status | Description |
|---|---|---|---|
| 1 | enum | C | Every `ChordQuality` enum value appears in `IconicChords.yaml` at least once |
| 2 | enum | T | Every `HarmonicFunction` value is emitted by at least one corpus voicing |
| 3 | enum | C | Every string in `.yaml` that names a `ChordQuality` / `ModalFamily` / `ScaleDeg… |
| 4 | enum | N | No enum value is dead: every variant is read somewhere (code or YAML) |
| 5 | catalog | T | Every `BinaryScaleId` in `WorldScales/*.yaml` exists in `ExtendedScales.yaml` w… |
| 6 | catalog | C | Every `Forte` number referenced in any YAML is in `ProgrammaticForteCatalog` |
| 7 | catalog | N | Every YAML cross-ref ID dereferences (no dangling FK) |
| 8 | catalog | C | Every `NeoRiemannian.yaml` triad name resolves to a valid `SetClass` |
| 9 | catalog | N | `IconicChords.yaml` pitch-class content matches its `quality` |
| 10 | round-trip | C | `PitchClassSet` → string → `PitchClassSet` is identity for all 4096 subsets |
| 11 | round-trip | C | `Voicing` → msgpack → `Voicing` is byte-identical |
| 12 | round-trip | N | F# YAML record → serialize → deserialize is field-identical |
| 13 | round-trip | T | C# `ChordName` → display string → parse produces same `ChordName` |
| 14 | algebraic | T | Neo-Riemannian P, L, R are involutions: T∘T = id |
| 15 | algebraic | C | PLR composites match: L∘P∘R = S, P∘L = N |
| 21 | cardinality | T | Triad template → exactly 3 distinct PCs after octave reduction |
| 22 | cardinality | T | Seventh template → exactly 4 distinct PCs |
| 23 | cardinality | C | Voicing MIDI count ≤ instrument string count |
| 25 | embedding | FAIL | Cross-instrument: voicings with identical PC-set have identical STRUCTURE parti… |
| 26 | embedding | C | MIDI-octave invariance: transposing by 12 leaves STRUCTURE vector unchanged |
| 27 | embedding | T | Embedding norm ∈ [0.99, 1.01] for all partitions (partition-normalized) |
| 28 | embedding | FAIL | No partition accuracy exceeds 1/3 + 3σ in 3-class leak test |
| 29 | schema | T | `SchemaHashV4` computed in C# == computed in Rust for same input |
| 30 | schema | C | OPTIC-K mmap header dims == `EmbeddingSchema.TotalDimensions` |
| 31 | schema | N | Dim count never changes without a corpus rebuild + new schema hash |
| 32 | text-vs-pc | FAIL | Same PC-set across octaves → cosine(STRUCTURE, STRUCTURE') == 1.0 |
| 33 | text-vs-pc | FAIL | `ChordName` consistency across instruments for same PC-set |
| 34 | governance | T | Every persona YAML has `affordances`, `goal_directedness`, `estimator_pairing` |
| 35 | governance | C | Every belief file is tetravalent-valid (T/F/U/C only) |

### Worst case: tested but never fired

These carry status `T` (tested) but observed zero firings. Either the test is vacuous or no exemplar exercises it.

- #2
- #5
- #13
- #14
- #21
- #22
- #27
- #29
- #34

## Catalog summary by status

- Tested (T): 9
- Claimed but untested (C): 12
- Latent / not claimed (N): 10
- Known failing (FAIL): 4

---
_Generated by `ix-invariant-coverage`._
